# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
aiofiles>=23.2.0

# Config
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Video & image
decord>=0.6.0
scenedetect[opencv]>=0.6.3
Pillow>=10.0.0
numpy>=1.24.0
imageio-ffmpeg>=0.4.9

# ASR (local SenseVoice-Small + FSMN-VAD via FunASR)
# Pin 1.2.6: funasr 1.3.x (incl. 1.3.9) ships a broken package — `model="fsmn-vad"`
# fails to build and `funasr.utils.modelscope_file` is missing. 1.2.6 resolves both.
funasr==1.2.6
einops>=0.7.0  # required by SenseVoice; not pulled in transitively
soundfile>=0.12.1

# OCR (slide / whiteboard text via RapidOCR — PP-OCRv4 weights on onnxruntime,
# avoids PaddlePaddle SIGILL on WSL2 + 13th-gen Intel without AVX-512)
rapidocr_onnxruntime>=1.4.4

# Retrieval encoders (local)
torch>=2.1.0
transformers>=4.40.0
accelerate>=1.0.0
FlagEmbedding>=1.2.10
chromadb>=0.4.20
modelscope>=1.20.0

# VLM API client
httpx>=0.27.0

# Eval dataset download (LongVideoBench / NExT-GQA annotations)
huggingface_hub>=0.24.0

# LangGraph / LangMem (LangMem still needs openai SDK via langchain-openai)
openai>=1.40.0
langgraph>=1.2.0
langgraph-checkpoint-sqlite>=3.1.0
langchain-core>=1.4.0
langchain>=1.3.1
langchain-openai>=1.0.0
trustcall>=0.0.39
langmem==0.0.30

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
